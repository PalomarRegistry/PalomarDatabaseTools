# Extracted source

These modules are maintained in this public repository. `PalomarDatabase`
consumes an immutable wheel built here and retains only its private moderation
entry points and a temporary compatibility shim during migration.
